%table.table
  %thead
    %tr
      - if show_client
        %th Клиент
      %th Услуги
      %th Время
      %th Стоимость
  - appointments.group_by(&:status).each do |status, status_appointments|
    %tr
      %td{ :colspan => (show_client ? 4 : 3), :style => 'background: #ccc; text-align:center;' }= status_appointments.first.aasm_human_state
    - status_appointments.each do |appointment|
      = render appointment, :show_client => show_client
