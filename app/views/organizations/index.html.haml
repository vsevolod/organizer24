- content_for :header do
  %h2 Список организаций

- @activities.each do |activity|
  - if activity.organizations.any?
    %h3= activity.name
    %ul
      - activity.organizations.each do |org|
        %li
          = link_to org.name, root_url(subdomain: org.subdomain)
          - if org.subdomain.blank?
            = link_to "Записаться", [:calendar, org]
          - else
            = link_to 'Записаться', calendar_url(subdomain: org.subdomain)
