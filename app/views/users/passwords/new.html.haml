%h2 Забыли свой пароль?
= simple_form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post }) do |f|
  = devise_error_messages!
  = f.input :email
  %div= f.submit "Выслать инструкцию по восстановлению пароля"
= render "links"
